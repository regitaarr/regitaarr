name: Test Workflow

on:
  issues:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: |
        echo "Workflow is working!"
        echo "Issue title: ${{ github.event.issue.title }}"
